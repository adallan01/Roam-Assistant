{
  "name": "roam-assistant",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling and a jsdom regression harness for the Roam Assistant widget. The widget itself ships as roam-assistant.js with no build step required to run it.",
  "scripts": {
    "build": "python3 src/build.py",
    "test": "node test/run.js && node test/run-home.js && node test/run-hubs.js && node test/run-legacy.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
